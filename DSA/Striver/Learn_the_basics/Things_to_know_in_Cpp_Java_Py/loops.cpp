#include<bits/stdc++.h>
using namespace std;
int main() {
    // for, while and do-while loops
    int i;
    for(i = 1; i <= 10; i++){
        cout << "Hello World!!" << " " << i << endl;
    }
    cout << "the last value of i: " << i;
    
    /*int i = n;
    while(i < x) {
        CODE TO BE EXECUTED
        i++ / i-- ;  or any increment/decrement condition to exit the loop after some iterations
    }*/

    /*int i = n;
    do{
        CODE TO BE EXECUTED(this will be executed for atleast once, no matter the condition for the running of the loop is.)
        increment/decrement condition on 'i'
    } while(condition on 'i')*/
    return 0;
}