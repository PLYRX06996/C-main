#include<iostream>
//there are two types of header files 
/*1.system defined header files: it comes with the compiler
2.user defined header files: it is created by the user*/
#include "this.h"/*If "this.h" is not in the same directory as the current file,
it will throw an error!*/
using namespace std;
int main() 
{
    cout<<"operators in C++ are following:"<<endl;
    /*Arithematic operators: +,-,*,/,%,++,--,**,*/
    /*Assifnment operator: '=' */
    /*Comparison operator: '==', '!=', '<', '>', '<=', '>=' */
    /*Logical operator: '&&', '||' '!' */
    return 0;
} 
